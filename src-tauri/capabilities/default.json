{
  "identifier": "default",
  "description": "Default capability for s-writor",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open",
    "opener:default",
    "opener:allow-reveal-item-in-dir",
    "store:default"
  ]
}
